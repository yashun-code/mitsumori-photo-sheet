# 計画書: 見積もり写真シート作成ツール

> これはプロジェクトの**芯**。作成した企画担当AI（Claude）だけが更新する。他AIは読むだけ。
> 細かい作業の進捗・担当は `指示書.md` 側に書く。ここは「決定事項」だけを置く。

## 背景・課題
- 本業で外注業者へ見積もり依頼を出す際、メールで説明文と写真を個別に送っている
- 説明文と写真の対応が伝わるよう、番号をふるなど手作業で工夫している
- 写真が複数枚になると容量の関係で何通にも分けて送ることになる

## ゴール
現場名・住所・作業メモと複数の写真を入力すると、テンプレートに自動で差し込まれて
1枚（複数ページ）のシート画像になるWebアプリを作る。写真と説明文の対応が一目でわかり、
メール1通で送れる状態にする。

## 利用シーン
- 誰が：ユーザー本人（現場担当）
- いつ：現場調査の直後、外注業者へ見積もり依頼を出す前
- 何のために：写真と説明の対応を一目でわかる形にまとめ、送付の手間を減らす
- 端末：PC・スマホ両方（現場ではスマホ、事務作業はPCの可能性あり）

## スコープ
- **やる**：
  - 入力項目：現場名・住所（必須）、希望曜日・作業内容メモ（任意）
  - 写真の複数アップロード（クリック選択／ドラッグ&ドロップ）
  - サムネイル一覧をドラッグで並べ替え → 番号は並び順で自動更新
  - 各写真への一言メモ（任意）
  - A4横のシートへリアルタイムに差し込みプレビュー
  - 写真が6枚を超えたら自動で複数ページに分割
  - シートをPNG画像として書き出し・ダウンロード
  - 外注業者へ送るメール文面をその場で自動生成し、コピーできる
  - PC／スマホのレスポンシブ対応（スマホは1カラム縦積み、プレビューは横スクロール）
  - 処理はすべてブラウザ内（クライアントサイド）で完結し、写真や住所などのデータはサーバーに送信しない
  - GitHub Pages（非公開リポジトリ）で公開。URLを知っている人だけが使う想定で、追加のアクセス制限（パスワード等）は設けない
- **やらない**（今回は対象外）：
  - ログイン認証・アクセス制限
  - 入力内容のサーバー保存・複数端末間の同期（毎回その場で完結、ページを閉じたら消える前提）
  - AIによる自動処理（文章生成・画像解析等）— このツールにAIは使わない

## 設計の決定事項
- **技術スタック**：静的HTML/CSS/JS（フレームワークなし）。画像合成はCanvas API、PNG書き出しは `canvas.toBlob`
- **ホスティング**：GitHub Pages。非公開リポジトリを作成 → Pages有効化 → push で公開
- **配色**：水色ベース（見出し `#0d6e8f` 系、アクセント `#0e9bc4` 系）＋写真番号バッジはオレンジ（`#e0672c`）
- **レイアウト**：
  - PC：左＝入力パネル（380px固定）／右＝プレビュー（可変幅）の2カラム
  - スマホ（768px以下）：1カラム縦積み（入力パネル→プレビューの順）、入力欄は16px以上（iOS自動ズーム回避）、プレビューのシートは横スクロールで縮小しすぎず表示
- **データ永続化**：なし（MVP）。必要になれば入力内容のlocalStorage一時保存を後日検討

## フェーズの進捗（ゲート）
- [x] フェーズ0 起動 — 課題・ゴール合意（2026-08-21）
- [x] フェーズ1 設計・モック — レイアウト／配色／レスポンシブまで合意（2026-08-21）
- [ ] フェーズ2 指示書作成
- [ ] フェーズ3 実装（Codexへ委譲）
- [ ] フェーズ4 レビュー・動作確認
- [ ] フェーズ5 GitHub Pages公開

## 関連ファイル
- /Users/shunya.m/Desktop/AIドライブ/projects/mitsumori-photo-sheet/mockup/mockup_v1.html — 合意済みモックアップ（PC／スマホ両対応）
- /Users/shunya.m/Desktop/AIドライブ/projects/mitsumori-photo-sheet/指示書.md — 作業台（実装ステップの正本）
