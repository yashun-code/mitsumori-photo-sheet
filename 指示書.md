# 指示書: 見積もり写真シート作成ツール

> これは作業台。どのAIも更新してよい（着手前に担当欄へ自分の名前を記入）。
> 決定事項の正本は `計画書.md`。ここに書く内容が計画書と矛盾する場合は計画書を優先する。

## 担当
- 実装：Codex（着手時に記入）

## 参照
- 計画書：/Users/shunya.m/Desktop/AIドライブ/projects/mitsumori-photo-sheet/計画書.md
- 合意済みモックアップ：/Users/shunya.m/Desktop/AIドライブ/projects/mitsumori-photo-sheet/mockup/mockup_v1.html
  （PC＝2カラム、768px以下＝1カラム縦積み。配色・フォントサイズ・余白はこのモックアップに準拠する）

## 技術方針
- 静的サイト（フレームワークなし）。`index.html` / `style.css` / `script.js` の構成
- 画像合成・PNG書き出しは Canvas API で直接描画する（DOMスクリーンショット系のライブラリは使わない。依存を増やさずオフラインでも確実に同じ見た目で出力するため）
- サーバー通信・外部API呼び出しは一切なし。すべてブラウザ内で完結させる

## 機能仕様

### 1. 入力フォーム
- 現場名（必須）、住所（必須）、希望曜日（任意）、作業内容メモ（任意・複数行）
- 現場名か住所が未入力の場合、PNGダウンロードとメール文面コピーのボタンは無効化し、
  「現場名と住所を入力してください」のような案内を出す

### 2. 写真アップロード・並べ替え
- 複数ファイル選択（`<input type="file" multiple accept="image/*">`）とドラッグ&ドロップの両方に対応
- アップロードした写真はサムネイル一覧に追加され、順番どおりに 1, 2, 3... と番号が自動でふられる
- 並べ替え：PCはドラッグで順序変更。スマホはドラッグの実装が不安定になりやすいため、
  各サムネイル行に「↑ ↓」ボタンを併設し、タップでも順序変更できるようにする
  （PC・スマホ共通でこのボタンを出してよい）
- 各サムネイル行に一言メモ欄（任意）と削除ボタンを置く
- 削除・並べ替えのたびに番号を振り直す

### 3. プレビュー
- A4横比率（297:210）のシートに、現場名・住所・希望曜日・作業内容メモ（ヘッダー部）と
  写真グリッド（3列×2行、各写真に番号バッジ）をリアルタイムに反映する
- 写真が7枚以上になったら自動的に2ページ目を生成し、7〜12枚目を配置する（以降も6枚ごとに増える）
- ページが複数あるときはタブ（モックアップの「1ページ目」「2ページ目」）で切り替えて確認できる

### 4. PNG書き出し
- 各ページを Canvas に実サイズ（300dpi相当、モックアップ参照PNGと同程度の解像度）で描画し、
  `canvas.toBlob` で PNG としてダウンロードする
- ページが複数ある場合は、ページごとに個別のダウンロードボタンを表示する（zip化はしない）
- ファイル名は `現場写真一覧_{現場名}_{ページ番号}.png` のような形にする

### 5. メール文面生成
- 「メール文面をコピー」を押すと、以下のような文面を組み立ててクリップボードにコピーする
  （文言は仮でよいので、後で1箇所で編集しやすいようテンプレート文字列をまとめておく）
  ```
  【見積もり依頼】{現場名}

  お世話になっております。
  下記現場の見積もりをお願いいたします。

  現場名：{現場名}
  住所：{住所}
  希望曜日：{希望曜日}

  作業内容：
  {作業内容メモ}

  現場写真は添付の画像をご確認ください（写真の番号と上記メモの番号が対応する場合はここに記載）。

  よろしくお願いいたします。
  ```

### 6. レスポンシブ
- モックアップの `@media (max-width: 768px)` の方針をそのまま踏襲する
  （1カラム化、入力欄フォントサイズ16px以上、プレビューは横スクロール、タップ領域を広めに）

## 受け入れ条件
- [ ] 現場名・住所未入力ではPNG／メール文面の操作ができない
- [ ] 写真を6枚アップロード→1ページのプレビューになる
- [ ] 写真を7枚以上アップロード→自動で2ページ目が増え、両ページとも個別にPNGダウンロードできる
- [ ] 並べ替え（PC：ドラッグ、スマホ：↑↓ボタン）で番号が正しく振り直される
- [ ] スマホ幅（375px程度）で1カラム表示になり、主要な操作がタップで問題なく行える
- [ ] ダウンロードしたPNGが、プレビュー画面の見た目と一致している
- [ ] 外部通信が発生していない（ネットワークタブで確認）

## 公開手順（実装完了後）
1. このプロジェクトフォルダをリポジトリのルートとして、非公開GitHubリポジトリ `mitsumori-photo-sheet` を作成
2. `main` ブランチにpush
3. GitHub Pagesを有効化（`main` ブランチ / ルート、または `/docs` — 実装時にどちらか選び、ここに追記する）
4. 発行されたURLを指示書の「変更ログ」に記録する

## 変更ログ
- 2026-08-21 Claude：計画書・指示書を作成。モックアップ（PC＋スマホ対応、水色配色）まで合意済み
